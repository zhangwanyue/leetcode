//
// Created by vera on 18-7-16.
//
#include <iostream>
#include <vector>
#include <algorithm>
#define N 10
#define M 20
using namespace std;

//int main() {
//    //二维数组
//    vector< vector<int> > Matrix(N, vector<int>(M, -1));
//    cout<< Matrix[0][0] << endl;
//    //pairs可以直接被排序
//    vector<pair<double, pair<int, int> > > pairs(2);
//    pairs[0] = make_pair(0.1, make_pair(1, 1));
//    pairs[1] = make_pair(0.2, make_pair(2, 2));
//    sort(pairs.begin(), pairs.end());
//    cout<< pairs[0].first << " " << pairs[0].second.first << " " << pairs[0].second.second << endl;
//}
